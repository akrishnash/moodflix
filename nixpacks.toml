# Nixpacks configuration for Railway
# Ensures both Node.js and Python are available

[phases.setup]
nixPkgs = ["nodejs-18_x", "python39"]

[phases.install]
cmds = [
  "npm install",
  "pip install -r requirements.txt"
]

[start]
cmd = "bash start.sh"

